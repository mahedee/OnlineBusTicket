@model IEnumerable<OnlineBusTicket.Models.Bus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.objCompany.companName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.busNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.busCapacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.busStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.objCompany.companName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.busNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.busCapacity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.busStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.busId }) |
            @Html.ActionLink("Details", "Details", new { id=item.busId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.busId })
        </td>
    </tr>
}

</table>
